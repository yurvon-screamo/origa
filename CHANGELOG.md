# Changelog

Источник поля «Что нового» в RuStore: джоба `upload-rustore`
(`.github/workflows/_upload-rustore.yml`) перед загрузкой AAB извлекает
секцию текущей версии из этого файла скриптом
`.github/scripts/extract-whatsnew.sh`.

## Формат

- Заголовок секции — `## X.Y.Z`: ровно номер тега без `v`, без даты. Новейшая секция сверху.
- Внутри секции — буллеты (дефис и пробел), по-русски; суммарно ≤500 символов на секцию (лимит поля «Что нового» RuStore; скрипт считает символы и падает при превышении).
- Запрещены: ссылки, инлайн-код, вложенные заголовки, эмодзи — витрина RuStore это не переваривает.
- Секция версии должна быть заполнена **до пуша stable-тега**: отсутствующая или пустая секция роняет `upload-rustore` до создания черновика. Recovery при падении — ручной ресабмит через RuStore Console (см. AGENTS.md → Git).

## 0.7.32

## 0.7.31

- Полировка интерфейса: убрали лишнюю подсказку на этапе ответа со звуком, сделали компактнее заголовок в грамматике и шапку прогресса на главном экране.
- Обновление сессии и синхронизация стали стабильнее — меньше ложных ошибок и потерянных отчётов о сбоях.

## 0.7.30

- Технический выпуск без новых функций: обновили внутренние компоненты и повысили стабильность приложения.

## 0.7.29

- Новое в повторении: карточки, в которых вы ошиблись, возвращаются на доработку, пока не закрепятся, и больше не дублируются в уроке.
- День повторения теперь считается по вашему местному времени.
- Вход и регистрация (включая Яндекс) стали надёжнее при нестабильной сети, сообщения об ошибках входа — понятнее.
- Синхронизация работает быстрее, статистика на главном экране обновляется сразу.

## 0.7.28

- Технический выпуск без новых функций: обновили внутренние компоненты, исправили ошибки и повысили стабильность работы приложения.

## 0.7.27

- Обновили грамматику: новые шаблоны употребления и более точные варианты ответов в квизах.
- Списки слов и фраз помнят выбранные фильтры и место, где вы остановились.
- Страница фраз открывается быстрее.
- Улучшили знакомство со словом в уроках и кнопку «назад».
- Исправили ошибки онбординга и сбои синхронизации.
